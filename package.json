{
  "name": "chrome-notifications2",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "compile": "tsc --noEmit",
    "dev": "vite",
    "build": "rimraf dist && vite build"
  },
  "devDependencies": {
    "@types/webextension-polyfill": "^0.10.0",
    "chrome-types": "^0.1.307",
    "rimraf": "^6.0.1",
    "typescript": "^5.3.2",
    "vite": "^5.0.0",
    "vite-plugin-web-extension": "^4.0.0",
    "webextension-polyfill": "^0.10.0"
  }
}